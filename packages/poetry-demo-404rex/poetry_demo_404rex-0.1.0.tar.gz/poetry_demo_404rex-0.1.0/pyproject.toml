[tool.poetry]
name = "poetry-demo-404rex"
version = "0.1.0"
description = ""
authors = ["Deven <devendraa769@gmail.com>"]
readme = "README.md"
packages = [{include = "poetry_demo"}]

[tool.poetry.dependencies]
python = "^3.8"
starlette = "^0.28.0"
uvicorn = "^0.22.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
