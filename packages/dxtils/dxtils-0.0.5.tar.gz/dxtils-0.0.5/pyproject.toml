[build-system]
requires = ["setuptools>=61.0", "undetected_chromedriver", "selenium", "webdriver-manager==3.5.4"]
build-backend = "setuptools.build_meta"

[project]
name = "dxtils"
version = "0.0.5"
description = "Utilities..."
requires-python = ">=3.10"
readme = "README.md"