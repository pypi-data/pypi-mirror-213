[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "ShadowPT"
authors = [{name = "gcf", email = "pg50398@alunos.uminho.pt"}, {name = "hjfa", email = "pg50414@alunos.uminho.pt"}]
readme = "README.md"
license = {file = "LICENSE"}
classifiers = ["License :: OSI Approved :: MIT License"]
dynamic = ["version", "description"]
dependencies = ["requests", "bs4", "argparse" , "spacy"]

[project.scripts]
shadow = "ShadowPT:main"

[project.urls]
Home = "https://github.com/Miyagi61/SPLN-TP2"