[tool.poetry]
name = "icap_commons"
version = "0.1.0"
description = "A repostory to store command code shared across all inride microservices"
authors = ["Your Name <you@example.com>"]
license = "MIT"
readme = "README.md"
packages = [{include = "icap_commons"}]

[tool.poetry.dependencies]
python = "^3.10"
fastapi = "^0.95.1"
loguru = "^0.7.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
