[tool.poetry]
name = "signway-sdk"
version = "0.1.3"
description = "Create signed URLs for Signway"
authors = ["gabotechs <gb.mt.me@gmail.com>"]
readme = "README.md"
packages = [{ include = "signway_sdk" }]

[tool.poetry.dependencies]
python = "^3.9"
requests = "^2.31.0"

[tool.poetry.dev-dependencies]
pytest = "^7.3.1"
flake8 = "^6.0.0"
requests = "^2.31.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

