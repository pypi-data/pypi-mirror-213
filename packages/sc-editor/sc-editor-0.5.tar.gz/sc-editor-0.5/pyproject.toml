[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
addopts = ["--cov=SC_editor"]
testpaths = ["tests"]

[tool.mypi]
mypi_path = "src"