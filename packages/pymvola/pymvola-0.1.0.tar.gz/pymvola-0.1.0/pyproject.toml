[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "pymvola"
version = "0.1.0"
authors = [
  { name="ryuka", email="lovanirina.ran@gmail.com" },
]
description = "Open source and easy use of the MVola API"
readme = "README.md"
requires-python = ">=3.7"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]
dependencies = [
  'requests',
]