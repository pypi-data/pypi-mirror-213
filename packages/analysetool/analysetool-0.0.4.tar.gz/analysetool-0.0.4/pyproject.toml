[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"


[project]
name = "analysetool"
version = "0.0.4"
dependencies = [
    "requests",
    'importlib-metadata; python_version<"3.8"',
]

[tool.setuptools]
#py-modules = ["waterquality"]