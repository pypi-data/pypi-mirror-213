[build-system]
build-backend = "setuptools.build_meta"
requires = ["setuptools", "cython"]

[project]
name = "libcythonconst"
version = "1.0.0"
description = "An example library with const in Cython"
requires-python = ">=3.11"
