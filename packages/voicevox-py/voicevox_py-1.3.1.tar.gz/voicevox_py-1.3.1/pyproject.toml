[tool.poetry]
name = "voicevox-py"
version = "1.3.1"
description = "Make VOICEVOX available on Python."
authors = ["sonyakun"]
license = "MIT"
readme = "README.md"
packages = [{include = "src/"}]

[tool.poetry.dependencies]
python = "^3.10"
httpx = "^0.24.1"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
