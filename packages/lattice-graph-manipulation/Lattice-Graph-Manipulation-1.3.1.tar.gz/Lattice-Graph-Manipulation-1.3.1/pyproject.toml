[tool.pdm]

[tool.setuptools.packages.find]
exclude = [
    "Lattice_Scripts*",
]

[project]
name = "Lattice-Graph-Manipulation"
version = "1.3.1"
description = ""
authors = [
    { name = "MII", email = "mnel.jnr@gmail.com" },
]
dependencies = [
    "graphviz>=0.20.1",
]
requires-python = ">=3.9"
readme = "README.md"

[project.license]
text = "MIT"

[build-system]
requires = [
    "pdm-pep517>=1.0",
]
build-backend = "pdm.pep517.api"
